[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "tmart"
version = "0.1.0"
requires-python = ">=3.11"

[tool.setuptools]
packages = []

[tool.fastapi-cli.run]
app = "main:app"